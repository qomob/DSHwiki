# Smoke composition — real cordis loader + real dsh-tools pipeline + this
# plugin + a driver that executes the tools. Run from plugin/: npm run smoke
- name: '@deepseek-ai/dsh-system-prompt'
- name: '@deepseek-ai/dsh-tools'
- name: './driver.js'
- name: '../index.js'
